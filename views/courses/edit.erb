Do you want to change? 

<form action="/courses/<%= @course.id %>" method="post" id="edit">
			<input type="hidden" name="_method" value="put" />
 
    
    	<input type="text"  id="name" name="course[name]" value="<%= @course.name %>" / ><br>
    	<input type="text"  id="name" name="course[start_date]" value="<%= @course.start_date.strftime("%b %d, %Y") %>" / ><br>
    	
    	<br>
    
    	<input type="submit" value="Change!" /><br><br>
   </form>